{
  "name": "compact-jev",
  "version": "0.4.2",
  "description": "/compact-jev: Jev-guided verbatim compaction for Claude Code, called through Vercel AI Gateway. Never replaces /compact or auto-compaction.",
  "author": {
    "name": "edoproch",
    "url": "https://github.com/edoproch"
  },
  "license": "MIT",
  "userConfig": {
    "apiKey": {
      "type": "string",
      "title": "AI Gateway API key",
      "description": "Vercel AI Gateway API key. Leave unset to use AI_GATEWAY_API_KEY.",
      "sensitive": true
    },
    "keepThreshold": {
      "type": "number",
      "title": "Keep threshold",
      "description": "Minimum Jev probability that a tool call or tool result is still needed; below it the item is removed.",
      "default": 0.5
    },
    "preserveRecentMessages": {
      "type": "number",
      "title": "Recent messages to preserve",
      "description": "Number of newest messages pinned from compaction.",
      "default": 6
    },
    "maxStateTokens": {
      "type": "number",
      "title": "Maximum Jev state tokens",
      "description": "Estimated token budget for the conversation state sent with every Jev request.",
      "default": 25000
    },
    "maxRequestTokens": {
      "type": "number",
      "title": "Maximum Jev request tokens",
      "description": "Estimated token budget for state plus questions in one Jev request.",
      "default": 30000
    },
    "truncateHeadChars": {
      "type": "number",
      "title": "Truncated tool result head",
      "description": "Characters of a tool result kept when Jev decides its full output is no longer needed; 0 keeps only the note.",
      "default": 300
    },
    "model": {
      "type": "string",
      "title": "Jev model",
      "description": "AI Gateway evaluation model id.",
      "default": "typesafe-ai/jev"
    }
  }
}
